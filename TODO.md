# GOAL: Add multiple selection checkboxes and collection dropdown to image galleries

- Fix linter and reverse auto-save setting to true in .vscode/settings.json
- ~~Add button to AddToCollectionModal, which will allow users to enter multi-select mode~~
- Add multi-select state to component above AddToCollectionModal
- Have modal control state
- On state change, add the dropdown floating div and checkboxes
- Incorporate backend calls to get status, add to collection, and remove from collection
